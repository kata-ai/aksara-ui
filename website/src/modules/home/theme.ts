import { ThemeAttributes, variables } from '@kata-kit/theme';

const homepageTheme: ThemeAttributes = {
  backgroundColor: variables.colors.kataBlue,
  textColor: variables.colors.gray10,
  headingColor: variables.colors.white
};

export default homepageTheme;
