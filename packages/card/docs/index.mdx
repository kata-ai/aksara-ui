import { Fragment } from 'react';
import { Example, Props } from '@kata-kit/doc-utils';

import { Card, CardGrid, DashboardCard } from '../src/index';
import { cardProps, cardGridProps } from './props';
import { Button } from '../../button/src/index';
import { Dropdown, DropdownMenu, DropdownItem, DropdownToggle } from '../../dropdown/src/index';
import { variables } from '../../theme/src/index';

## Card

This is the base card component in use for all card-based UI elements.

<Example title="Basic Example" scope={{ Card, Fragment, variables }} code={require('../examples/0-basic-example.raw')} />

We have a special `<DashboardCard />` component for card elements in dashboard. It will force the card into a fixed height.

<Example title="Dashboard Card" scope={{ Card, CardGrid, DashboardCard, Fragment }} code={require('../examples/1-dashboard-cards.raw')} />

You can specify an `action` prop to add an icon button with any action.

<Example
  title="With Action"
  scope={{
    Card,
    CardGrid,
    Button,
    DashboardCard,
    Dropdown,
    DropdownMenu,
    DropdownItem,
    DropdownToggle,
    Fragment
  }}
  code={require('../examples/2-with-action.raw')}
/>

### Card Props

These props apply to both `Card` and `DashboardCard` components.

<Props props={cardProps} />

## CardGrid

Dashboard card are displayed in a gridded manner. The `CardGrid` component can help with that.

<Example title="Card Grid" scope={{ Card, CardGrid, DashboardCard, Fragment }} code={require('../examples/3-card-grid.raw')} />

<Example title="Card Grid with 2 row and centered" scope={{ Card, CardGrid, DashboardCard, Fragment }} code={require('../examples/4-card-grid-centered.raw')} />

### CardGrid Props

<Props props={cardGridProps} />
