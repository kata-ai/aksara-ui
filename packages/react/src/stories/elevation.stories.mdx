import { Meta, Story } from '@storybook/addon-docs';
import { ElevationGrid, ElevationDetail } from '../utils/storybook';
import { Card } from '../components';
import { Box } from '../layout';

<Meta title="Foundations/Elevation" />

# Elevation

This page contains Elevation levels, the distance between two surface where content and components live. The distance from the front of one surface to the front of another is measured along the z-axis and portrayed using shadows.

<Story id="core-foundations-elevation--elevation" />
