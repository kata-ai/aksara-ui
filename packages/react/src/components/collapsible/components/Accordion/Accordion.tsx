import { Accordion as ReachAccordion } from '@reach/accordion';
import styled from 'styled-components';

const Accordion = styled(ReachAccordion)`
  display: block;
`;

Accordion.displayName = 'Accordion';

export default Accordion;
