import { Fragment } from 'react';
import { Example, Props } from '@kata-kit/doc-utils';

import { Pagination } from '../src/index'
import { paginationProps } from './props'

Paginations allow navigating between a series of related contents which exist across multiple pages.

## Examples

<Example title="Basic Example" scope={{ Pagination, Fragment }} code={require('../examples/0-basic-example.raw')} />

Paginations will also automatically truncate pages in the most convenient manner when the number of pages get too large.

<Example title="Large Page Numbers" scope={{ Pagination, Fragment }} code={require('../examples/1-large-page-numbers.raw')} />

## Pagination Props

<Props props={paginationProps} />
