import { Fragment } from 'react'
import { Example, Props } from '@kata-kit/doc-utils';

import { InputText, InputTextarea, FormGroup, FormLabel, FormError } from '../src/index';
import { inputProps, formProps } from './props'

This component contains form elements that are used within kata-kit

## Form Inputs

Form elements currently supported by `kata-kit` are `InputText` and `InputTextarea`.

<Example
  title="Basic Example"
  scope={{ InputText, InputTextarea, FormGroup, FormLabel, FormError, Fragment }}
  code={require('../examples/0-basic-example.raw')}
/>

### Input Props

All Input props (`InputText`, `InputTextarea`) extend from their respective React attributes, with some additions.

<Props props={inputProps} />

## Form Elements

Combine the form elements above with `FormGroup`, and `FormLabel` to make up a nice form layout.

<Example
  title="Basic Example"
  scope={{ InputText, InputTextarea, FormGroup, FormLabel, FormError, Fragment }}
  code={require('../examples/1-form-layout.raw')}
/>

Add an `errors` prop for notifying any form elements/inputs with errors.

<Example
  title="Error States"
  scope={{ InputText, InputTextarea, FormGroup, FormLabel, FormError, Fragment }}
  code={require('../examples/2-error-states.raw')}
/>

### Form Props

The following are props for `FormLabel`, `FormGroup`, and `FormError` components. All components' props extend from their respective React attributes.

<Props props={formProps} />
