import { Fragment } from 'react'
import { Example, Props } from '@kata-kit/doc-utils';

import { InputText, InputTextarea } from '../src/index';

This component contains form elements that are used within kata-kit

## Components

* `FormGroup`
* `FormError`
* `FormLabel`
* `InputText`
* `InputTextarea`
* `InputSelect` (w/ `react-select` v2) [TODO]

<Example title="Basic Example" scope={{ InputText, InputTextarea, Fragment }} code={require('../examples/0-basic-example.raw')} />
