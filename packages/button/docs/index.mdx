import { Fragment } from 'react';
import { Example, Props } from '@kata-kit/doc-utils';

import { Button, FloatingButton } from '../src/index'
import { buttonProps, floatingButtonProps } from './props';

Buttons express what action will occur when the user clicks or touches it.
Buttons are used to initialize an action, either in the background or foreground of an experience.

# Button Component
<Example
  title="Basic Button Example"
  scope={{ Button, Fragment }}
  code={require('../examples/0-basic-example.raw')}
/>

## Button Props
<Props props={buttonProps} />

# Floating Button Component
<Example
  title="Floating Button Example"
  scope={{ FloatingButton, Fragment }}
  code={require('../examples/1-floating-example.raw')}
/>

## Floating Button Props
<Props props={floatingButtonProps} />
