import { Fragment } from 'react';
import { Example, Props } from '@kata-kit/doc-utils';

import {
  Button,
  FloatingButton,
  ToggleButton,
  SupportButton
} from '../src/index'
import {
  buttonProps,
  floatingButtonProps,
  toggleButtonProps,
  supportButtonProps
} from './props';

Buttons express what action will occur when the user clicks or touches it.
Buttons are used to initialize an action, either in the background or foreground of an experience.

## Basic Button
<Example
  title="Basic Button Example"
  scope={{ Button, Fragment }}
  code={require('../examples/0-basic-button.raw')}
/>

### Button Props
<Props props={buttonProps} />


## Floating Button

Floating buttons are buttons with styles similar to that of cards, hence "floating".

<Example
  title="Floating Button Example"
  scope={{ FloatingButton, Fragment }}
  code={require('../examples/1-floating-button.raw')}
/>

### Floating Button Props
<Props props={floatingButtonProps} />


## Toggle Button

The `<ToggleButton />` is a special helper component to render different button states on components with toggle states.

<Example
  title="Toggle Button Example"
  scope={{ ToggleButton, Button, Fragment }}
  code={require('../examples/2-toggle-button.raw')}
/>

### Toggle Button Props
<Props props={toggleButtonProps} />


## Support Button

Support buttons are subtle buttons, used for secondary actions on a form, card, etc.

<Example
  title="Support Button Example"
  scope={{ SupportButton, Button, Fragment }}
  code={require('../examples/3-support-button.raw')}
/>

### Support Button Props
<Props props={supportButtonProps} />
