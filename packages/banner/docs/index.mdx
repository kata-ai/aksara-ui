import { Fragment } from 'react';
import { Example, Props } from '@kata-kit/doc-utils';

import { Banner } from '../src/index';
import { bannerProps } from './props';

Banners are used to convey important information to users.

<Example title="Basic Example" scope={{ Banner, Fragment }} code={require('../examples/0-basic-example.txt')} />

Banners can also be closable.

<Example title="Closable Banners" scope={{ Banner, Fragment }} code={require('../examples/1-closable-banners.txt')} />

## Banner Props

<Props props={bannerProps} />
